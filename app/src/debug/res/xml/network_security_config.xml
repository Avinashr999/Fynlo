<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override of the main network security config.
  Release keeps cleartext disabled and trusts only system certificates.
  Debug builds also allow user-installed certificates and local Firebase
  emulator traffic for instrumented tests.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
