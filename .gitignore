# Android
*.iml
.gradle/
/local.properties
/build/
/app/build/
/macrobenchmark/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/

# IntelliJ / Android Studio
/.idea/
.DS_Store

# Keystore / secrets (never commit these)
*.jks
*.keystore
keystore.properties

# Gradle wrapper jar (optional – keeps repo lighter)
gradle/wrapper/gradle-wrapper.jar

# OS
Thumbs.db
# Signing keystore - NEVER commit this
cashmemo-release.jks
*.jks
*.keystore

# Firebase config — contains project credentials
google-services.json

# Room schema exports are committed to track DB changes
# Do NOT add /app/schemas to .gitignore
.claude/settings.local.json
secrets/

# Firebase emulator runtime logs / data
firebase-debug.log
firestore-debug.log
*-debug.log
.firebase/

# Node deps for the Firestore rules tests
firestore-tests/node_modules/
