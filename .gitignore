# Android
*.iml
.gradle/
.kotlin/
/local.properties
/build/
/app/build/
/macrobenchmark/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/

# IntelliJ / Android Studio
/.idea/
.DS_Store

# Keystore / secrets (never commit these)
*.jks
*.keystore
keystore.properties

# NOTE: gradle/wrapper/gradle-wrapper.jar IS committed â€” it's the version-pinned
# Gradle bootstrap that ./gradlew needs to run. Excluding it breaks CI and any
# fresh clone (see INF01 in UX_AUDIT_2026-05-25.md).

# OS
Thumbs.db
# Signing keystore - NEVER commit this
cashmemo-release.jks
*.jks
*.keystore

# Firebase config â€” contains project credentials
google-services.json

# Room schema exports are committed to track DB changes
# Do NOT add /app/schemas to .gitignore
.claude/settings.local.json
secrets/

# Firebase emulator runtime logs / data
firebase-debug.log
firestore-debug.log
*-debug.log
.firebase/

# Node deps for the Firestore rules tests
firestore-tests/node_modules/

# C22 smoke screenshots — avoid accidental commits
/v*.png
/check.png
/add_rec.png
/recheck.png
/reports.png
/moneyflow*.png
/fynlo_check.png

# All repo-root PNGs (ad-hoc screencaps) — drawables stay tracked under app/src/main/res/drawable/
/*.png
